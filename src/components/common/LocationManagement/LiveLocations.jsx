import React from 'react';

const LiveLocations = () => {
    return (
        <div>
            <h1>Welcome to the LiveLocations component!</h1>
        </div>
    );
};

export default LiveLocations;